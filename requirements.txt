# Versions figees sur celles reellement installees dans .venv le
# 05/09/2026 (verifie via .venv/Lib/site-packages/*.dist-info) - une
# dependance qui publie une version incompatible ne doit plus pouvoir
# casser un build du jour au lendemain. A remettre a jour volontairement,
# jamais par un simple `pip install --upgrade`.
shiny==1.7.0
pandas==3.0.5
psycopg2-binary==2.9.12
openpyxl==3.1.5
python-dotenv==1.2.3
mcp==1.29.1
chatlas[anthropic]==0.23.0

# Graphiques de l'assistant IA (voir assistant/rendu.py) : PNG trace cote
# serveur et integre a la carte du chat, jamais de fichier sur le disque.
matplotlib>=3.8,<4

# Assistant IA : comprehension des noms mal ecrits (centres, mois, tiers) -
# voir assistant/comprehension.py.
rapidfuzz>=3.0,<4

# Version du composant de chat verifiee avec l'assistant reconstruit (cartes
# de resultat ToolResultDisplay). Installee aujourd'hui comme dependance de
# shiny ; figee ici pour qu'une mise a jour ne change pas le rendu.
shinychat==0.6.1

# Hachage des codes d'acces (voir logic/donnees.py : ajouter_utilisateur,
# tenter_connexion).
bcrypt>=4.0

# Tests automatises (voir tests/).
pytest>=7.0
httpx>=0.27,<1
